import * as React from "react";

class Dashboard extends React.Component {
  public render() {
    return (
      <nav>
        <div>Dashboard</div>
      </nav>
    );
  }
}

export default Dashboard;
